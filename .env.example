# ========================================================
# FMCG Admin Panel & Booker API - Environment Variables
# ========================================================

# Server Configuration
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# Persistent External Database Connection (PostgreSQL - Supabase / Neon / Railway)
# Supabase: DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require"
# Neon:     DATABASE_URL="postgresql://[USER]:[PASSWORD]@[ENDPOINT].neon.tech/[DATABASE]?sslmode=require"
# Railway:  DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/railway"
DATABASE_URL="postgresql://postgres:password@localhost:5432/fmcg_admin?schema=public"

# JWT Secret for Mobile Booker API Tokens
JWT_SECRET="fmcg_super_secret_jwt_key_2026_prod"

# Master Admin Portal Credentials (Login at /login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123
