# ========================================================
# FMCG Admin Panel & Booker API - Environment Variables
# ========================================================

# Server Configuration
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# SQLite Database Connection (Inside Docker container)
DATABASE_URL="file:/app/prisma/dev.db"

# JWT Secret for Mobile Booker API Tokens
JWT_SECRET="fmcg_super_secret_jwt_key_2026_prod"

# Master Admin Portal Credentials (Login at /login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123
